# Expected Assumptions

- Status should default to `draft`.
- Owner should default to `unknown` unless supplied by the user.
- Prototype platform may be suggested as PC, but production platforms remain
  unresolved.
- Unity is assumed because this example selects the Unity game workflow.
- Shared Unity runtime use is a suggestion, not a confirmed dependency.
- Telemetry events are suggested from the loop and need human review.
