# Assumptions

- `sharedUnity` is a candidate dependency alias, not an approved standard.
- Dream Extraction likely needs save, telemetry, and liveops runtime
  capabilities.
- Space Runners likely needs telemetry and addressables capabilities.
- Shared runtime package version and exported record list are unresolved.
- Cross-workspace references are future VeritySpec concepts and require human
  review before implementation.
