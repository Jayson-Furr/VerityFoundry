# Questions

- Which shared Unity runtime version should consumers target?
- Which records are public/exported versus internal/private?
- Should dependency aliases be standardized across the studio?
- What lockfile shape should make validation reproducible?
- Which compatibility rules apply when the shared runtime changes?
- Which consumer games are affected if the telemetry client changes?
